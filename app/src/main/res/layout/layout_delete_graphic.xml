<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:visibility="gone"
    >

    <ImageView
        android:id="@+id/deleteGraphicBtn"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_marginBottom="@dimen/margin_large_xxx"
        android:background="@drawable/bg_btn_border_circle"
        android:contentDescription="@null"
        android:padding="@dimen/padding_medium"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:elevation="@dimen/medium"
        android:outlineSpotShadowColor="@color/black"
        android:outlineAmbientShadowColor="@color/black"
        app:tint="@color/white"
        tools:ignore="UnusedAttribute" />

    <TextView
        android:id="@+id/lbDelete"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/drag_to_delete"
        android:layout_marginBottom="@dimen/margin_medium"
        android:textSize="16sp"
        app:layout_constraintBottom_toTopOf="@id/deleteGraphicBtn"
        app:layout_constraintStart_toStartOf="@id/deleteGraphicBtn"
        app:layout_constraintEnd_toEndOf="@id/deleteGraphicBtn"
        android:shadowRadius="2"
        android:shadowColor="@color/black"
        android:textColor="@color/white"
        />
</androidx.constraintlayout.widget.ConstraintLayout>